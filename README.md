
  # Build this feature

  This is a code bundle for Build this feature. The original project is available at https://www.figma.com/design/NGYtoHd7zfAHaenaboEtyM/Build-this-feature.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  